import { View, Text } from 'react-native';

export default function JobForum() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Jobs Posted:</Text>
      {/* Add more components here to show job postings */}
    </View>
  );
}
